<div class="flex items-center">
  <% actions.each do |action| %>
    <%= link_to action[:url],
                class: action[:class],
                title: action[:title],
                data: action[:data] || {} do %>
      <%= send(action[:icon]) %>
    <% end %>
  <% end %>
</div>
