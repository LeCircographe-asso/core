<div id="event-<%= event.id %>-interest-count" class="flex items-center gap-2 text-sm text-gray-500">
  <i class="fas fa-users text-brand-primary" aria-hidden="true"></i>
  <span><%= count %> intéressé<%= count != 1 ? "s" : "" %></span>
</div>
