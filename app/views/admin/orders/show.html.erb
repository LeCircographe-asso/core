
<div id="product-container" data-controller="product-container"><div id="product-container">
  <% if @order.products.any? { |product| product.product_type == "adhesion" } %>
    <%= render "product_subscription" %>
  <% else %>
    <%= render "product_membership" %>
  <% end %>
</div>
</div>
<%= render "payment_and_donation" %>

<%= turbo_frame_tag "product_container" %>
