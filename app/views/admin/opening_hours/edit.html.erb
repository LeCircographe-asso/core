<% content_for :title, "Modification des horaires d'ouverture" %>

<div class="min-h-screen py-3" data-controller="opening-hours-editor">
  <div class="page-container">
    <!-- Breadcrumbs -->
    <%= render 'shared/breadcrumbs' %>
    
    <!-- Header -->
    <div class="surface-card mb-4">
      <div class="bg-gradient-to-r from-[#1F5C55] to-teal-600 px-6 py-2.5">
        <div class="flex flex-col sm:flex-row justify-between items-center gap-4">
          <div>
            <h1 class="text-xl font-bold text-white">Modifier les horaires d'ouverture</h1>
            <p class="text-white/80 text-sm">Ces horaires seront affichés sur le site public</p>
            <div class="mt-2">
              <span class="inline-flex items-center px-2 py-1 rounded text-xs font-medium bg-white/20 text-white">
                <i class="fas fa-calendar-week mr-1"></i>
                Semaine du <%= Date.current.beginning_of_week.strftime("%d/%m") %> au <%= Date.current.end_of_week.strftime("%d/%m/%Y") %>
              </span>
            </div>
          </div>
          <div class="text-white text-sm bg-white/20 px-3 py-1.5 rounded-lg">
            <div class="flex items-center">
              <i class="fas fa-info-circle mr-2"></i>
              <span>Utilisez les sélecteurs ou cochez "Fermé" pour chaque jour</span>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Fermeture exceptionnelle -->
    <div class="surface-card mb-4">
      <div class="px-4 py-3 border-b border-gray-200">
        <h2 class="text-base font-medium text-gray-900 flex items-center gap-2">
          <i class="fas fa-umbrella-beach text-amber-500"></i>
          Fermeture exceptionnelle
        </h2>
        <p class="text-xs text-gray-500 mt-1">
          Affiche un bandeau "Fermé" sur les pages publiques. Purement visuel — n'empêche jamais d'enregistrer une présence ou un paiement en interne (ouverture ponctuelle pendant les vacances, etc.).
        </p>
      </div>
      <div class="px-4 py-3">
        <%= form_with url: admin_exceptional_closure_path, method: :patch, class: "flex flex-wrap items-end gap-4" do %>
          <label class="inline-flex items-center gap-2 text-sm font-medium text-gray-700">
            <%= check_box_tag :active, "1", @exceptional_closure.active? %>
            Fermeture active
          </label>
          <div>
            <label class="block text-xs text-gray-500 mb-1" for="label">Libellé (optionnel)</label>
            <%= text_field_tag :label, @exceptional_closure.label, placeholder: "Vacances d'été", class: "border border-gray-300 rounded-md px-2 py-1.5 text-sm" %>
          </div>
          <div>
            <label class="block text-xs text-gray-500 mb-1" for="ends_on">Jusqu'au (optionnel)</label>
            <%= date_field_tag :ends_on, @exceptional_closure.ends_on, class: "border border-gray-300 rounded-md px-2 py-1.5 text-sm" %>
          </div>
          <%= submit_tag "Enregistrer", class: "px-4 py-1.5 text-sm font-medium text-white bg-amber-600 rounded-md hover:bg-amber-700 focus:outline-none focus:ring-2 focus:ring-offset-0 focus:ring-amber-500" %>
        <% end %>
      </div>
    </div>

    <!-- Main content in two columns -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-4">
      <!-- Left column - Form -->
      <div class="surface-card">
        <div class="px-4 py-2 border-b border-gray-200 flex items-center justify-between">
          <h2 class="text-base font-medium text-gray-900">Formulaire de modification</h2>
          <div class="flex gap-2">
            <%= link_to admin_opening_hours_path, class: "text-gray-500 hover:text-gray-700" do %>
              <i class="fas fa-arrow-left"></i> Retour
            <% end %>
          </div>
        </div>
        
        <div class="p-3">
          <!-- Message d'erreur global -->
          <% if @error_message %>
            <div class="bg-red-50 border-l-4 border-red-400 p-4 mb-4">
              <div class="flex">
                <div class="flex-shrink-0">
                  <i class="fas fa-exclamation-circle text-red-400"></i>
                </div>
                <div class="ml-3">
                  <p class="text-sm text-red-700">
                    <%= @error_message %>
                  </p>
                </div>
              </div>
            </div>
          <% end %>
          
          <!-- Form -->
          <%= form_with url: admin_opening_hours_path, method: :patch, id: "opening-hours-form" do |form| %>
            <div class="overflow-hidden rounded-lg border border-gray-200">
              <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                  <tr>
                    <th scope="col" class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider w-1/3">Jour</th>
                    <th scope="col" class="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider w-2/3">Horaires</th>
                  </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200 text-sm">
                  <% @opening_hours.each do |day, hours| %>
                    <tr class="hover:bg-gray-50">
                      <td class="px-3 py-2 whitespace-nowrap">
                        <div class="flex items-center">
                          <% icon_class = case day 
                             when "lundi", "mardi", "mercredi", "jeudi", "vendredi" then "fas fa-briefcase"
                             when "samedi", "dimanche" then "fas fa-umbrella-beach"
                             end %>
                          <i class="<%= icon_class %> text-gray-500 mr-2 text-xs"></i>
                          <span class="font-medium text-gray-700"><%= day.capitalize %></span>
                        </div>
                      </td>
                      <td class="px-3 py-2 whitespace-nowrap">
                        <div class="space-y-2">
                          <!-- Bouton Fermé -->
                          <div class="flex items-center mb-2">
                            <%= check_box_tag "closed_#{day}", "1", hours.to_s.downcase == "fermé", 
                                class: "closed-checkbox h-4 w-4 text-red-600 border-gray-300 rounded focus:ring-red-500", 
                                data: {
                                  day: day,
                                  opening_hours_editor_target: "closedCheckbox",
                                  action: "change->opening-hours-editor#toggleClosed"
                                },
                                checked: hours.to_s.downcase == "fermé" %>
                            <%= label_tag "closed_#{day}", "Fermé", class: "ml-2 text-sm text-gray-700" %>
                          </div>
                          
                          <!-- Sélecteurs d'heures modernes -->
                          <div class="time-input" data-day="<%= day %>" data-opening-hours-editor-target="timeInput" style="<%= 'display: none;' if hours.to_s.downcase == 'fermé' %>">
                            <div class="flex items-center space-x-2 text-sm">
                              <!-- Ouverture -->
                              <div class="flex items-center space-x-1">
                                <select name="open_hour_<%= day %>" class="hour-select bg-white border border-gray-300 rounded px-2 py-1 text-sm focus:border-[#1F5C55] focus:ring-1 focus:ring-[#1F5C55] appearance-none cursor-pointer" data-day="<%= day %>" data-action="change->opening-hours-editor#updateFromSelectors">
                                  <% (0..23).each do |h| %>
                                    <% selected = hours.to_s.downcase != "fermé" && hours.split(' - ').first&.split(':')&.first&.to_i == h %>
                                    <option value="<%= h %>" <%= 'selected' if selected %>><%= h.to_s.rjust(2, '0') %></option>
                                  <% end %>
                                </select>
                                <span class="text-gray-500">:</span>
                                <select name="open_minute_<%= day %>" class="minute-select bg-white border border-gray-300 rounded px-2 py-1 text-sm focus:border-[#1F5C55] focus:ring-1 focus:ring-[#1F5C55] appearance-none cursor-pointer" data-day="<%= day %>" data-action="change->opening-hours-editor#updateFromSelectors">
                                  <% [0, 15, 30, 45].each do |m| %>
                                    <% selected = hours.to_s.downcase != "fermé" && hours.split(' - ').first&.split(':')&.last&.to_i == m %>
                                    <option value="<%= m %>" <%= 'selected' if selected %>><%= m.to_s.rjust(2, '0') %></option>
                                  <% end %>
                                </select>
                              </div>
                              
                              <span class="text-gray-500">-</span>
                              
                              <!-- Fermeture -->
                              <div class="flex items-center space-x-1">
                                <select name="close_hour_<%= day %>" class="hour-select bg-white border border-gray-300 rounded px-2 py-1 text-sm focus:border-[#1F5C55] focus:ring-1 focus:ring-[#1F5C55] appearance-none cursor-pointer" data-day="<%= day %>" data-action="change->opening-hours-editor#updateFromSelectors">
                                  <% (0..23).each do |h| %>
                                    <% selected = hours.to_s.downcase != "fermé" && hours.split(' - ').last&.split(':')&.first&.to_i == h %>
                                    <option value="<%= h %>" <%= 'selected' if selected %>><%= h.to_s.rjust(2, '0') %></option>
                                  <% end %>
                                </select>
                                <span class="text-gray-500">:</span>
                                <select name="close_minute_<%= day %>" class="minute-select bg-white border border-gray-300 rounded px-2 py-1 text-sm focus:border-[#1F5C55] focus:ring-1 focus:ring-[#1F5C55] appearance-none cursor-pointer" data-day="<%= day %>" data-action="change->opening-hours-editor#updateFromSelectors">
                                  <% [0, 15, 30, 45].each do |m| %>
                                    <% selected = hours.to_s.downcase != "fermé" && hours.split(' - ').last&.split(':')&.last&.to_i == m %>
                                    <option value="<%= m %>" <%= 'selected' if selected %>><%= m.to_s.rjust(2, '0') %></option>
                                  <% end %>
                                </select>
                              </div>
                            </div>
                            
                            <!-- Champ caché pour la valeur finale -->
                            <%= hidden_field_tag "opening_hours[#{day}]", hours, 
                                class: "final-time-input", 
                                data: { day: day, opening_hours_editor_target: "finalInput" } %>
                          </div>
                          
                          <div class="text-xs text-red-500 hidden error-message mt-1" data-error-for="<%= day %>"></div>
                        </div>
                      </td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            </div>

            <div class="flex justify-end gap-2 mt-3">
              <%= form.submit "Enregistrer", class: "px-4 py-1.5 text-sm font-medium text-white bg-[#1F5C55] rounded-md hover:bg-[#194A45] focus:outline-none focus:ring-2 focus:ring-offset-0 focus:ring-[#1F5C55]" %>
            </div>
          <% end %>
        </div>
      </div>
      
      <!-- Right Column - Preview -->
      <div class="surface-card">
        <div class="px-4 py-2 border-b border-gray-200">
          <h2 class="text-base font-medium text-gray-900">Aperçu des horaires</h2>
        </div>
        <div class="p-3">
          <div id="preview-container" data-opening-hours-editor-target="previewContainer">
            <%= render 'shared/opening_hours', schedule: @opening_hours, closure: @exceptional_closure %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<style nonce="<%= content_security_policy_nonce %>">
/* Masquer les flèches natives et ajouter une icône moderne */
.hour-select, .minute-select {
  -webkit-appearance: none;
  -moz-appearance: none;
  appearance: none;
  background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' fill='none' viewBox='0 0 20 20'%3e%3cpath stroke='%236b7280' stroke-linecap='round' stroke-linejoin='round' stroke-width='1.5' d='M6 8l4 4 4-4'/%3e%3c/svg%3e");
  background-position: right 8px center;
  background-repeat: no-repeat;
  background-size: 16px;
  padding-right: 32px;
}

.hour-select:focus, .minute-select:focus {
  background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' fill='none' viewBox='0 0 20 20'%3e%3cpath stroke='%231F5C55' stroke-linecap='round' stroke-linejoin='round' stroke-width='1.5' d='M6 8l4 4 4-4'/%3e%3c/svg%3e");
}

.hour-select:hover, .minute-select:hover {
  border-color: #1F5C55;
  box-shadow: 0 0 0 1px rgba(31, 92, 85, 0.1);
}
</style>
