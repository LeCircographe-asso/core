# Configuration Kamal pour Production - VPS Ionos Linux M
service: circographe-production
image: lecircographe-asso/circographe

# Serveur VPS Ionos
servers:
  web:
    hosts:
      - <%= ENV.fetch("PRODUCTION_SERVER_IP") %>

# Configuration SSH pour Kamal
ssh:
  user: deploy

# Credentials for your image host
registry:
  server: ghcr.io
  username: LeCircographe-asso
  password:
    - KAMAL_REGISTRY_PASSWORD

# Proxy configuration pour production (kamal-proxy avec Let's Encrypt auto)
proxy:
  ssl: true
  host: lecircographe.fr
  app_port: 80  # ← PORT INTERNE de l'app dans le conteneur
  # SSL géré automatiquement par kamal-proxy via Let's Encrypt

# Variables d'environnement production
env:
  secret:
    - RAILS_MASTER_KEY
  clear:
    RAILS_ENV: production
    PORT: 80  # ← FORCER Puma à écouter sur port 80
    PRODUCTION_MODE: true
    # Site pas encore ouvert au public : tout est bloqué (503) sauf /up,
    # y compris /session/new et /admin (voir MaintenanceModeMiddleware).
    # Ouverture prévue le 18/09 : repasser à false + redeploy ce jour-là.
    MAINTENANCE_MODE: true
    # Autorisation d'indexation Google (X-Robots-Tag, voir ApplicationController).
    # Activée dès maintenant : sans effet tant que MAINTENANCE_MODE bloque tout
    # (rien à indexer), mais évite d'avoir un 2e flag à changer le jour J.
    SEO_INDEXABLE: true
    RAILS_SERVE_STATIC_FILES: true
    SOLID_QUEUE_IN_PUMA: true
    # Réplication continue SQLite → IONOS Object Storage (config/initializers/litestream.rb).
    # Credentials via config/credentials.yml.enc (litestream:), déchiffrées avec RAILS_MASTER_KEY
    # déjà présent ci-dessus — pas de nouveau secret Kamal à ajouter.
    LITESTREAM_REPLICATE_IN_PUMA: true
    # Optimisations pour VPS M
    WEB_CONCURRENCY: 0  # ← Single mode (pas de workers)
    JOB_CONCURRENCY: 1
    RAILS_LOG_LEVEL: info

# Volumes pour les logs et données (production)
volumes:
  - "circographe_production_storage:/rails/storage"
  - "/srv/www/lecircographe_production/log:/app/log"

# Bridge fingerprinted assets
asset_path: /rails/public/assets

# Configure the image builder
builder:
  arch: amd64
