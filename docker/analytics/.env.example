# Copy to .env (never commit the real .env) and fill with generated secrets:
#   openssl rand -hex 32   -> UMAMI_APP_SECRET
#   openssl rand -hex 24   -> UMAMI_DB_PASSWORD

UMAMI_DB_PASSWORD=
UMAMI_APP_SECRET=
